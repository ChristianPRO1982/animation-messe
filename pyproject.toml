[project]
name = "animation-messe"
version = "0.1.0"
description = "Gestion de l'animation liturgique paroissiale"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "django>=6.0.3",
    "psycopg[binary]>=3.2.6",
]
